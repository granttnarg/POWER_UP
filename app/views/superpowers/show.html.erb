<%= render 'shared/navbar' %>
<div class="show-conatiner" style="text-align: center;padding-top: 95px;">
    <br>
      <h1 style="padding-bottom: 25px;">
        <strong><%= @superpower.name %></strong>
      </h1>
        <div><%= cl_image_tag @superpower.photo, height: 300, crop: :fill %>
        </div>
        <div style="font-size: 40px; padding: 5px;"><%= @superpower.description %>
        </div>
        <div style="font-size: 40px; padding: 5px;"><%= "€#{@superpower.price} per day" %>
        </div>
          <li class = "inline"><p>User name: <%= @superpower.user.name %></p>
          </li>
          <li class = "inline">
            <p>Book: <%= @superpower.user.email %></p>
          </li>

  <p><%= link_to "Click to make a Booking", new_superpower_booking_path(@superpower), class: 'btn btn-secondary' %></p>
  <p><%= link_to "Back to All Superpowers", superpowers_path, class: 'btn btn-outline-dark' %></p>
  <div class="edit-delete">

  </div>
  <div>
    <h2>Reviews:</h2>
    <p>
      <% @superpower.reviews.each do |review| %>
        <% review.rating.times do  %>
          <span class="fas fa-star"></span>
        <% end %>
        <% review.blank_stars.times do  %>
          <span class="far fa-star"></span>
        <% end %>
        <br />
        <%= "\"#{review.description}\"" %>
      <br />
      <br />
      <% end%>
    </p>

  </div>

  </div>
